/*

    querySelector(): Returns the first element within the document
                     that matches the specified CSS selector.

    querySelectorAll(): Returns all elements within the document that
                        match the specified CSS selector, as a static
                        NodeList.

*/